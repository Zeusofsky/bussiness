import { FC, ReactElement } from 'react';
import { Box } from '@mui/material';


const SiteVisitReport : FC = (): ReactElement => {
    return (
      <Box>
        Site Visit Report
      </Box>
    );
};

export default SiteVisitReport