import { FC, ReactElement } from 'react';
import { Box } from '@mui/material';


const managementReport : FC = (): ReactElement => {
    return (
      <Box>
        management Report
      </Box>
    );
};

export default managementReport