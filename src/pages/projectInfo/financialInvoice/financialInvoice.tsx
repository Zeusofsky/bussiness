import { FC, ReactElement } from 'react';
import { Box } from '@mui/material';


const InvoiceFinancial : FC = (): ReactElement => {
    return (
      <Box>
        InvoiceFinancial
      </Box>
    );
};

export default InvoiceFinancial